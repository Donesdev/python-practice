"""First we define the function definition by using the 
reserved keyword "def" to define it. Then set up the function with threee 
parameters firstName, middleInitial, lastName 
and then to end a funtion, we place a colon at the end"""

# def myFunctions(firstName, middleInitial, lastName):
#     # finally we will print to screen so we can see it work
#     print(firstName,middleInitial,lastName)
# # here is where we call myFunction and pass it the actual value as arguments
# myFunctions("Jose", "A", "Dones")


""" Len() Function """
"""We can calculate and print out the length or the number of 
characters in a string using the len() function."""

myString = 'Python is fun!'
print(myString.title())